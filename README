== Setting Up

1. Setting up Environment
	make sure bundle is installed (gem install bundle)
	bundle install
	rake db:schema:dump
2. Populating Data (always seed first)
  rake db:seed
  Geolocation data => see lib/script_geoname.rb
  Eventful data => see lib/script_eventful.rb (Infinite script. Control-c to stop fetching.)
  Attraction data => (TODO)

  

  

	